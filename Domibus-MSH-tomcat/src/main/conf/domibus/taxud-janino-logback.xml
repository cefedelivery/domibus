<?xml version="1.0" encoding="UTF-8"?>

<included>

    <appender name="taxud-file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${catalina.home}/logs/taxud-domibus.log</file>

            <filter class="ch.qos.logback.core.filter.EvaluatorFilter">      
                <evaluator> <!-- defaults to type ch.qos.logback.classic.boolex.JaninoEventEvaluator -->
                    <expression>
                        if (mdc == null || mdc.get("d_domain") == null)
                            return false;

                        String domain = (String) mdc.get("d_domain");

                        if ("taxud".equals(domain)) 
                          return true;
                        
                        return false;
                    </expression>
                </evaluator>
                <onMismatch>DENY</onMismatch>
                <onMatch>ACCEPT</onMatch>
            </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- rollover daily -->
            <fileNamePattern>${catalina.home}/logs/taxud-domibus-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- each file should be at most 30MB, keep 60 days worth of history, but at most 20GB -->
            <maxFileSize>30MB</maxFileSize>
            <maxHistory>60</maxHistory>
            <totalSizeCap>20GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{ISO8601} [%X{d_user}] [%X{d_domain}] [%X{d_messageId}] %5p %c{1}:%L - %m%n</pattern>
        </encoder>
    </appender>

    <logger name="eu.domibus" level="INFO" additivity="false">
        <appender-ref ref="taxud-file" />
    </logger>

</included>