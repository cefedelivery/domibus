<?xml version="1.0" encoding="UTF-8"?>

<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>eu.domibus</groupId>
        <artifactId>domibus</artifactId>
        <version>3.3.1-SNAPSHOT</version>
    </parent>
    <packaging>pom</packaging>
    <artifactId>domibus-distribution</artifactId>
	<name>Domibus MSH Distribution</name>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-assembly-plugin</artifactId>
                    <version>2.4</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>default-plugins</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>make-assembly-default-plugins</id>
                                <configuration>
                                    <descriptors>
                                        <descriptor>src/main/assembly/default-plugins/default-fs-plugin.xml</descriptor>
                                        <descriptor>src/main/assembly/default-plugins/default-ws-plugin.xml</descriptor>
                                        <descriptor>src/main/assembly/default-plugins/default-jms-plugin.xml</descriptor>
                                    </descriptors>
                                </configuration>
                                <!-- bound to verify because we need to execute cargo-package first
                                    which is in pre-integration-test -->
                                <phase>verify</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>tomcat</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>

            <properties>
                <tomcat.version>8.0.24</tomcat.version>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.cargo</groupId>
                        <artifactId>cargo-maven2-plugin</artifactId>
                        <version>1.4.12</version>
                        <executions>
                            <execution>
                                <id>build-tomcat</id>
                                <goals>
                                    <goal>configure</goal>
                                    <goal>package</goal>
                                </goals>
                                <phase>pre-integration-test</phase>
                                <configuration>
                                    <container>
                                        <containerId>tomcat8x</containerId>
                                        <zipUrlInstaller>
                                            <url>
                                                https://ec.europa.eu/cefdigital/artifact/service/local/repositories/eDelivery/content/org/apache/tomcat/tomcat/${tomcat.version}/tomcat-${tomcat.version}.zip
                                            </url>
                                        </zipUrlInstaller>
                                    </container>
                                    <packager>
                                        <outputLocation>
                                            ${project.build.directory}/artifacts/domibus-container
                                        </outputLocation>
                                    </packager>
                                    <configuration>
                                        <files>
                                            <file>
                                                <file>${project.basedir}/../Domibus-MSH-tomcat/src/main/conf/domibus</file>
                                                <todir>conf/domibus</todir>
                                            </file>
                                        </files>
                                    </configuration>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>make-assembly-tomcat</id>
                                <configuration>
                                    <descriptors>
                                        <descriptor>src/main/assembly/tomcat/tomcat-configuration-only.xml</descriptor>
                                        <descriptor>src/main/assembly/tomcat/tomcat-war.xml</descriptor>
                                        <descriptor>src/main/assembly/tomcat/tomcat-full.xml</descriptor>
                                    </descriptors>
                                </configuration>
                                <!-- bound to verify because we need to execute cargo-package first
                                    which is in pre-integration-test -->
                                <phase>verify</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>weblogic</id>

            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>make-assembly-weblogic</id>
                                <configuration>
                                    <descriptors>
                                        <descriptor>src/main/assembly/weblogic/weblogic-configuration-only.xml</descriptor>
                                        <descriptor>src/main/assembly/weblogic/weblogic-war.xml</descriptor>
                                    </descriptors>
                                </configuration>
                                <!-- bound to verify because we need to execute cargo-package first
                                    which is in pre-integration-test -->
                                <phase>verify</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>wildfly</id>
            <properties>
                <wildfly.version>9.0.2.Final</wildfly.version>
            </properties>

            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.cargo</groupId>
                        <artifactId>cargo-maven2-plugin</artifactId>
                        <version>1.4.15</version>
                        <executions>
                            <execution>
                                <id>build-wildfly</id>
                                <goals>
                                    <goal>configure</goal>
                                </goals>
                                <phase>pre-integration-test</phase>
                                <configuration>
                                    <container>
                                        <containerId>wildfly9x</containerId>
                                        <zipUrlInstaller>
                                            <url>
                                                https://ec.europa.eu/cefdigital/artifact/content/repositories/eDelivery/org/wildfly/as/wildfly-as-dist/9.0.2.Final/wildfly-as-dist-9.0.2.Final.zip
                                            </url>
                                        </zipUrlInstaller>
                                    </container>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>com.google.code.maven-replacer-plugin</groupId>
                        <artifactId>replacer</artifactId>
                        <version>1.5.3</version>
                        <executions>
                            <execution>
                                <id>domibus-conf-location</id>
                                <phase>post-integration-test</phase>
                                <goals>
                                    <goal>replace</goal>
                                </goals>
                                <configuration>
                                    <includes>
                                        <include>
                                            ${project.build.directory}/cargo/installs/wildfly-as-dist-${wildfly.version}/wildfly-${wildfly.version}/bin/standalone.conf.bat
                                        </include>
                                        <include>
                                            ${project.build.directory}/cargo/installs/wildfly-as-dist-${wildfly.version}/wildfly-${wildfly.version}/bin/standalone.conf
                                        </include>
                                    </includes>
                                    <replacements>
                                        <!-- Please keep the values from the "token" and the "value" fields on one line and do not include any whitespaces -->
                                        <replacement>
                                            <token>"JAVA_OPTS=-Xms64M -Xmx512M -XX:MaxPermSize=256M"</token>
                                            <value>"JAVA_OPTS=-Xms64M -Xmx512M -XX:MaxPermSize=256M -Ddomibus.config.location=%JBOSS_HOME%/conf/domibus"</value>
                                        </replacement>
                                        <!-- Please keep the values from the "token" and the "value" fields on one line and do not include any whitespaces -->
                                        <replacement>
                                            <token>JAVA_OPTS="-Xms64m -Xmx512m -XX:MaxPermSize=256m -Djava.net.preferIPv4Stack=true"</token>
                                            <value>JAVA_OPTS="-Xms64m -Xmx512m -XX:MaxPermSize=256m -Djava.net.preferIPv4Stack=true -Ddomibus.config.location=\$JBOSS_HOME/conf/domibus"</value>
                                        </replacement>
                                    </replacements>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>make-assembly-wildfly</id>
                                <configuration>
                                    <descriptors>
                                        <descriptor>src/main/assembly/wildfly/wildfly-configuration-only.xml</descriptor>
                                        <descriptor>src/main/assembly/wildfly/wildfly-war.xml</descriptor>
                                        <descriptor>src/main/assembly/wildfly/wildfly-full.xml</descriptor>
                                    </descriptors>
                                </configuration>
                                <!-- bound to verify because we need to execute cargo-package first
                                    which is in pre-integration-test -->
                                <phase>verify</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>sample-configuration</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>make-assembly-sample-configuration</id>
                                <configuration>
                                    <descriptors>
                                        <descriptor>src/main/assembly/sampleconfiguration/sample-configuration-and-testing.xml</descriptor>
                                    </descriptors>
                                </configuration>
                                <!-- bound to verify because we need to execute cargo-package first
                                    which is in pre-integration-test -->
                                <phase>verify</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>database</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>make-assembly-sqlscripts</id>
                                <configuration>
                                    <descriptors>
                                        <descriptor>src/main/assembly/sqlscripts/sql-scripts.xml</descriptor>
                                    </descriptors>
                                </configuration>
                                <!-- bound to verify because we need to execute cargo-package first
                                    which is in pre-integration-test -->
                                <phase>verify</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>